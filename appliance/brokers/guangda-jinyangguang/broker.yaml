# 光大证券 金阳光 QMT — the build validated end-to-end in the prototype.
# schema v1; all fields optional (shown explicitly here for clarity).
schema_version: 1

broker:
  id: guangda-jinyangguang
  name: 光大金阳光 QMT

terminal:
  client: bin.x64/XtItClient.exe
  userdata: userdata_mini

xtquant:
  path: xtquant            # xtquant_250807 placed at <pack root>/xtquant

mcp:
  mode: readonly
