"""Opt-in, read-only xttrade account-query tools (feature 004).

Off by default; enabled only with an explicit flag AND a server-side account
allowlist. Exposes ONLY query_* tools — never order/cancel/transfer/borrow/export.
"""
