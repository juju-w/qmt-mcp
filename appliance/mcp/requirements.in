# Direct runtime dependencies for the in-Wine Windows Python 3.12 server.
# requirements.txt is the complete transitive lock consumed by CI and Docker.

mcp==2.0.0
uvicorn==0.52.0
numpy==2.5.1
pandas==3.0.5
asyncpg==0.31.0  # 012: used only when QMT_DB_URL enables PostgreSQL persistence
